"this might be used for creating a database in a docker  and 
 storing randomly generate data to test the docker db,
 but this also might be done using terminal,js(with fs), and a csv file 
 so in the future this file might be deleted "